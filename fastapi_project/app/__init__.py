"""
LLM Router & Matching Platform

A production-ready system for:
- Parsing LLM metadata from Hugging Face
- Calculating VRAM requirements (weights + KV cache + overhead)
- Matching models to compatible hardware (GPU, SoC, mobile)
"""

__version__ = "1.0.0"
